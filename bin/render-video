#!/usr/bin/env node
import("../dist/render-video.mjs").catch((err) => {
  console.error("Failed to load render-video CLI:", err.message);
  console.error("Did you run 'npm run build' first?");
  process.exit(1);
});
